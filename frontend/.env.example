# Public project URL. Safe to expose to browser code when needed.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Server-only credential. Never prefix this variable with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=replace-with-local-or-hosted-service-role-key
