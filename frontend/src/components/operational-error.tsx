"use client";
import { AlertTriangle, RefreshCw } from "lucide-react";
import { useLocale } from "@/features/i18n/locale-provider";
export function OperationalError({ reset }: { error: Error; reset: () => void }) { const {t}=useLocale(); return <section role="alert" style={{display:"grid",maxWidth:"38rem",justifyItems:"start",gap:"0.65rem",margin:"3rem auto",border:"1px solid #efc4bf",borderRadius:"0.75rem",padding:"1rem",background:"#fff",color:"#687573"}}><AlertTriangle size={24}/><strong style={{color:"#7f302b"}}>{t("No pudimos cargar esta vista","We could not load this view")}</strong><span style={{fontSize:"0.72rem"}}>{t("La consulta autenticada falló. Puedes volver a intentarlo.","The authenticated query failed. You can try again.")}</span><button type="button" onClick={reset} style={{display:"inline-flex",minHeight:"2.75rem",alignItems:"center",gap:"0.4rem",border:0,borderRadius:"0.5rem",padding:"0.55rem 0.75rem",background:"#185c55",color:"#fff",fontWeight:800,cursor:"pointer"}}><RefreshCw size={16}/>{t("Reintentar","Retry")}</button></section>; }
